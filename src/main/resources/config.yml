# Death-banned 配置文件

# 数据库设置
database:
  path: "plugins/HardcoreRevive/data.db"

# 计分板设置
scoreboard:
  enabled: true
  update_interval: 5 # 更新间隔（秒）

# 墓碑设置
tombstone:
  enabled: true
  # 墓碑保留时间（秒），0表示永久
  duration: 0